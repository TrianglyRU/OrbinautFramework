/// @description Insert description here
// You can write your code in this editor

	/* We leave a blank code here to prevent tails render from their
	own side, since we're render them in player object */	
	
	// Note:
	// No code in Draw event means object won't render